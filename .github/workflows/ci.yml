name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Build backend
        run: mvn clean package
      - name: Run backend tests
        run: mvn test

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Clear npm cache
        run: npm cache clean --force
      - name: Install ChromeHeadless
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          echo "CHROME_BIN=/usr/bin/google-chrome" >> $GITHUB_ENV
      - name: Debug Chrome version
        run: google-chrome --version
      - name: Install dependencies
        run: npm ci
      - name: Install Protractor
        run: npm install -g protractor
      - name: Debug ChromeDriver download
        run: |
          curl -o chromedriver.zip https://storage.googleapis.com/chrome-for-testing-public/126.0.6478.126/linux64/chromedriver-linux64.zip
          file chromedriver.zip
          ls -l chromedriver.zip
      - name: Install ChromeDriver manually
        run: |
          curl -o chromedriver.zip https://storage.googleapis.com/chrome-for-testing-public/126.0.6478.126/linux64/chromedriver-linux64.zip
          unzip chromedriver.zip -d node_modules/protractor/node_modules/webdriver-manager/selenium/
          mv node_modules/protractor/node_modules/webdriver-manager/selenium/chromedriver-linux64/chromedriver node_modules/protractor/node_modules/webdriver-manager/selenium/chromedriver_126.0.6478.126
          chmod +x node_modules/protractor/node_modules/webdriver-manager/selenium/chromedriver_126.0.6478.126
      - name: Debug WebDriver
        run: ls -la node_modules/protractor/node_modules/webdriver-manager/selenium
      - name: List test files
        run: |
          echo "Unit test files:"
          find src/app -name "*.spec.ts"
          echo "E2E test files:"
          find e2e/src -name "*.e2e-spec.ts"
      - name: Debug Karma and Chrome
        run: |
          npx karma --version
          google-chrome --version
          npx ng version
          protractor --version
      - name: Build project
        run: npx ng build
      - name: Run unit tests
        run: npx ng test --browsers=ChromeHeadless --watch=false
        env:
          CHROME_BIN: /usr/bin/google-chrome
      - name: Kill existing processes on port 4200
        run: |
          fuser -k 4200/tcp || true
      - name: Start backend
        run: |
          cd ../backend
          mvn spring-boot:run &
          sleep 10
      - name: Start server for E2E tests
        run: |
          npx ng serve --port 0 --proxy-config proxy-conf.json &
          echo "E2E_BASE_URL=http://localhost:$(lsof -i :4200-4299 -t | head -1)/" >> $GITHUB_ENV
        env:
          CI: true
      - name: Wait for server
        run: sleep 10
      - name: Run E2E tests
        run: npx ng e2e
        env:
          CHROME_BIN: /usr/bin/google-chrome
      - name: Check test results
        if: always()
        run: |
          echo "Test results summary:"
          if [ -d "coverage" ]; then
            echo "Coverage report generated. Listing files:"
            ls -la coverage
            if [ -f "coverage/lcov.info" ]; then
              echo "Coverage summary:"
              cat coverage/lcov.info | grep -E '^(SF|LF|LH)' || echo "No coverage data"
            fi
          else
            echo "No coverage report generated."
          fi
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: frontend/coverage
          retention-days: 7
